<?xml version="1.0" encoding="utf-8"?>
<!-- Seção: item continuar assistindo (corrigido: inclui textos usados pelo adapter e acessibilidade) -->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="160dp"
    android:layout_height="wrap_content"
    android:layout_margin="8dp">

    <!-- Poster + overlays -->
    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="220dp">

        <ImageView
            android:id="@+id/imgPoster"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:scaleType="centerCrop"
            android:contentDescription="@string/movie_poster_description" />

        <View
            android:id="@+id/poster_overlay"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@color/poster_overlay"
            android:visibility="gone" />

        <ImageView
            android:id="@+id/btnPlay"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:layout_gravity="center"
            android:src="@drawable/ic_play"
            android:contentDescription="@string/watch_trailer"
            android:focusable="true"
            android:clickable="true"
            android:background="@color/card_overlay"
            android:padding="8dp" />

        <ImageView
            android:id="@+id/btnInfo"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:layout_gravity="bottom|end"
            android:src="@drawable/ic_info_outline"
            android:contentDescription="@string/info"
            android:focusable="true"
            android:clickable="true"
            android:background="@color/card_overlay"
            android:padding="12dp" />

    </FrameLayout>

    <!-- Texto informativo abaixo do poster -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:layout_gravity="bottom"
        android:paddingTop="6dp"
        android:paddingStart="4dp"
        android:paddingEnd="4dp"
        android:background="@android:color/transparent">

        <TextView
            android:id="@+id/txtTitle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/title_placeholder"
            android:textColor="@color/white"
            android:textSize="13sp"
            android:textStyle="bold"
            android:ellipsize="end"
            android:maxLines="1" />

        <TextView
            android:id="@+id/txtDescription"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/description_short"
            android:textColor="@color/white"
            android:textSize="12sp"
            android:ellipsize="end"
            android:maxLines="1" />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:layout_marginTop="2dp">

            <TextView
                android:id="@+id/txtInfoYear"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:text="@string/info_year_placeholder"
                android:textColor="@color/gray"
                android:textSize="11sp" />

            <TextView
                android:id="@+id/txtInfoAge"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/info_age_placeholder"
                android:textColor="@color/gray"
                android:textSize="11sp"
                android:textStyle="bold" />

        </LinearLayout>

    </LinearLayout>

</FrameLayout>
